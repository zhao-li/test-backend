.base:
  stage: pre-build-checks

unit-test-job:
  extends: .base
  script:
    - scripts/test_app.sh

lint-job:
  extends: .base
  script:
    - scripts/lint_app.sh

document-job:
  extends: .base
  script:
    - scripts/document_app.sh

