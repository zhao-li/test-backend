.base:
  &base
  stage: pre-build-checks

unit-test-job:
  <<: *base
  script:
    - scripts/test_app.sh

lint-job:
  <<: *base
  script:
    - env
    - scripts/lint_app.sh

document-job:
  <<: *base
  script:
    - env
    - export PATH="$CI_PROJECT_DIR/.cache/pip/bin/:$PATH"
    - env
    - scripts/document_app.sh
    - sphinx-build docs/source/ docs/build/
    - "$CI_PROJECT_DIR/sphinx-build docs/source/ docs/build/"
    - pwd

