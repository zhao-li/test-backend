.base:
  &base
  needs:
    - build-production-container
  stage: production-checks
  image: ${PRODUCTION_CONTAINER_IMAGE}
  before_script:
    - cd ${APP_DIR}
    - ls -la

integration-test:
  <<: *base
  needs:
    - build-production-container
    - unit-test
  extends:
    - .database
    - .cache-coverage
  before_script:
    - cd ${APP_DIR}
    - pip install
        coverage
        factory_boy
        unittest-xml-reporting
    - cp ${ARTIFACT_DESTINATION_DIR}/.coverage ${APP_DIR}/.
  script:
    - scripts/test_app/run_integration_tests.sh
    - coverage xml
    - coverage report
  after_script:
    - cp --recursive ${ARTIFACT_SOURCE_DIR}/ ${CI_PROJECT_DIR}/
  artifacts:
    reports:
      cobertura: ${ARTIFACT_DESTINATION_DIR}/coverage.xml
      junit: ${ARTIFACT_DESTINATION_DIR}/test.xml

